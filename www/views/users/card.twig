
            <td><a href='/purchaseconfirm/{{ order.id }}'>Commande n°{{ order.id }}</a></td>
            <td>{{ order.price }} €</td>
                <form action="/profil" method="POST">
                    <input type="hidden" name="id" value="{{order.id}}" />
                    <td><input type="submit" name="delete" value="Supprimer" /></td>
                </form>
            </tr>
